export const metadata = {
  title: "Cosa è Bloom",
  description: "Bloom è un centro di pedagogia moderna che accompagna le persone nel loro percorso di vita.",
};

export default function SiamoLayout({ children }) {
  return children;
} 